/**
 * Parse validation errors (numbers) into human-readable messages
 */
export const parseValidationErrors = (errors) => {
  // TODO: Parse validation errors
  switch (errors) {
    default:
      return errors;
  }
};
