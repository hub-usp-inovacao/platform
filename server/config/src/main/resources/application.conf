hub-usp {
  sheets {
    apiKey = ""
    apiKey = ${?HUB_GOOGLE_SHEETS_API_KEY}

    company {
      id = ""
      id = ${?HUB_COMPANY_SHEET_ID}

      tab = ""
      tab = ${?HUB_COMPANY_TAB_NAME}
    }

    discipline {
      id = ""
      id = ${?HUB_DISCIPLINE_SHEET_ID}

      tab = ""
      tab = ${?HUB_DISCIPLINE_TAB_NAME}
    }

    funds {
      id = ""
      id = ${?HUB_FUNDS_SHEET_ID}

      tab = ""
      tab = ${?HUB_FUNDS_TAB_NAME}
    }

    incubators {
      id = ""
      id = ${?HUB_INCUBATOR_SHEET_ID}

      tab = ""
      tab = ${?HUB_INCUBATOR_TAB_NAME}
    }

    initiative {
      id = ""
      id = ${?HUB_INITIATIVE_SHEET_ID}

      tab = ""
      tab = ${?HUB_INITIATIVE_TAB_NAME}
    }

    patent {
      id = ""
      id = ${?HUB_PATENT_SHEET_ID}

      tab = ""
      tab = ${?HUB_PATENT_TAB_NAME}
    }

    pdi = {
      id = ""
      id = ${?HUB_PDI_SHEET_ID}

      tab = ""
      tab = ${?HUB_PDI_TAB_NAME}
    }

    researcher {
      id = ""
      id = ${?HUB_RESEARCHER_SHEET_ID}

      tab = ""
      tab = ${?HUB_RESEARCHER_TAB_NAME}
    }

    companyRegisterForm {
      id = ""
      id = ${?HUB_COMPANY_REGISTER_FORM_ID}

      tab = ""
      tab = ${?HUB_COMPANY_REGISTER_FORM_NAME}
    }
  }

  email {
    devs = ""
    devs = ${?HUB_DEVS_EMAIL}

    username = ""
    username = ${?HUB_EMAIL_ADDRESS}

    password = ""
    password = ${?HUB_EMAIL_PASSWORD}
  }

  ktor {
    allowedHosts = "*"
    allowedHosts = ${?HUB_CORS_ALLOWED_HOSTS}
  }

  jwt {
    secret = "supersecretwithabout32characters"
    secret = ${?HUB_JWT_SECRET}

    issuer = "hubusp"
    issuer = ${?HUB_JWT_ISSUER}

    domain = "https://jwt-provider-domain/"
    domain = ${?HUB_JWT_DOMAIN}

    audience = "jwt-audience"
    audience = ${?HUB_JWT_AUDIENCE}

    realm = "ktor sample app"
    realm = ${?HUB_JWT_REALM}
  }

  datasource {
    protocol = "mongodb"
    protocol = ${?HUB_DATASOURCE_PROTOCOL}

    host = "localhost"
    host = ${?HUB_DATASOURCE_HOST}

    port = "27017"
    port = ${?HUB_DATASOURCE_PORT}

    dbName = "superdb"
    dbName = ${?HUB_DATASOURCE_DBNAME}
  }
}
