hub-usp {
  sheets {
    apiKey = "sheets api key"

    company {
      id = "company id"
      tab = "company tab"
    }

    discipline {
      id = "discipline id"
      tab = "discipline tab"
    }

    funds {
      id = "funds id"
      tab = "funds tab"
    }

    incubators {
      id = "incubators id"
      tab = "incubators tab"
    }

    initiative {
      id = "initiative id"
      tab = "initiative tab"
    }

    patent {
      id = "patent id"
      tab = "patent tab"
    }

    pdi = {
      id = "pdi id"
      tab = "pdi tab"
    }

    researcher {
      id = "researcher id"
      tab = "researcher tab"
    }

    companyRegisterForm {
      id = "company register form id"
      tab = "company register form tab"
    }
  }

  # TODO: Add (B)CC
  email {
    devs = "email devs"
    username = "email username"
    password = "email password"
  }

  ktor {
    allowedHosts = "allowed hosts"
  }

  jwt {
    secret = "jwt secret"
    issuer = "jwt issuer"
    domain = "jwt domain"
    audience = "jwt audience"
    realm = "jwt realm"
  }

  datasource {
    protocol = "datasource protocol"
    host = "datasource host"
    port = "datasource port"
    dbName = "datasource dbName"
  }
}
