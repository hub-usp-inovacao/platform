hub-usp {
  sheets {
    apiKey = "sheets api key"

    company {
      id = "company id"
      tab = "company tab"
    }

    discipline {
      id = "discipline id"
      tab = "discipline tab"
    }

    funds {
      id = "funds id"
      tab = "funds tab"
    }

    incubators {
      id = "incubators id"
      tab = "incubators tab"
    }

    initiative {
      id = "initiative id"
      tab = "initiative tab"
    }

    patent {
      id = "patent id"
      tab = "patent tab"
    }

    pdi = {
      id = "pdi id"
      tab = "pdi tab"
    }

    researcher {
      id = "researcher id"
      tab = "researcher tab"
    }

    companyRegisterForm {
      id = "company register form id"
      tab = "company register form tab"
    }

    companyUpdateForm {
      id = "company update form id"
      tab = "company update form tab"
    }
  }

  email {
    devs = "email devs"
    cc = "first@example.com,second@example.com,third@example.com,first@example.com"
    username = "email username"
    password = "email password"
  }

  ktor {
    allowedHosts = "0.0.0.0:8080,localhost:8080"
  }

  jwt {
    secret = "jwt secret"
    issuer = "jwt issuer"
    domain = "jwt domain"
    audience = "jwt audience"
    realm = "jwt realm"
  }

  database {
    protocol = "database protocol"
    host = "database host"
    port = "database port"
    dbName = "database dbName"
  }
}
