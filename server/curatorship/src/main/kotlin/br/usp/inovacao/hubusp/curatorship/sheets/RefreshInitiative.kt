package br.usp.inovacao.hubusp.curatorship.sheets

class RefreshInitiative {

    fun refresh() {
        // read and validate data (row) from spreadsheet
        // delete "old" documents
        // validate data
        // if is valid, persist Initiative in db
        // else, persist InitiativeVlidationError in db
    }
}